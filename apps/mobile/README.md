# apps/mobile

React Native / Expo phone app — Sallie's "body". Imported from the
predecessor `sallie-project` repo in Phase 2.

The Convergence onboarding UI (~40 questions, see
[`../../docs/spec/convergence.md`](../../docs/spec/convergence.md)) is
the first screen on first launch.

Currently empty — Phase 2 imports land here. The existing root-level
Expo app (`/App.tsx`, `/app/`, etc.) will be migrated into this folder
during Phase 2; for now it stays at the root to keep the tree
buildable.

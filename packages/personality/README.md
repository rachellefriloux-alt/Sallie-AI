# packages/personality

Unified persona: values, tone, voice register. Sourced from `before/`
and `sallie_1.0/` predecessor modules and merged with `Sallie/`'s
persona system in Phase 4.

Reads from `persona.json` (produced by Convergence — see
[`../../docs/spec/convergence.md`](../../docs/spec/convergence.md)).

Currently empty — Phase 4 lands here.

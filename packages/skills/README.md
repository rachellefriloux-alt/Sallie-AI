# packages/skills

Plugin / tool registry. Skills are the things Sallie *can do* in the
outside world — calendar, email, files, web search, code execution,
and so on.

Imported from `before/feature/` in Phase 6. Every skill invocation is
gated by user consent per the `sallie-infinite` spec.

Currently empty — Phase 6 lands here.

{
  "name": "@sallie/core",
  "version": "0.0.0",
  "private": true,
  "description": "Cross-surface shared kernel — types, DTOs, Zod schemas, and persona/memory/tone/identity primitives that mobile, web, desktop, and the brain all share. Contents are promoted phase-by-phase from host root dirs and from legacy/ — see README for the source map.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json"
  }
}
